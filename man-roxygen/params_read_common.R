#' @param clean if TRUE, then [mc_prep_clean] is called automatically while reading (default TRUE)
#' @param silent if TRUE, then any information is not printed in console (default FALSE)
#' @param user_data_formats sutom data formats - list(key=mc_DataFormat) [mc_DataFormat-class] (default NULL)
#'
#' If custom data format is defined the key can be used in data_format parameter in [mc_read_files()] and [mc_read_data()].
