#' @param use_utc §if FALSE, then `tz_offset` is used for output timeseries (default TRUE)
#'
#' Agg-format accept the FALSE value only for step shorter than day. In Raw-format isn't parameter limited.§
